# 内部管理路由 — 仅监听 Tailscale IP，HTTP 即可（Tailscale 链路已加密）
server {
    listen <TAILSCALE_IP>:80;

    location /admin/ {
        allow 127.0.0.1;
        allow 100.64.0.0/10;
        deny all;
        proxy_pass http://127.0.0.1:8001;
    }
}
