根据用户问题和参考内容，提取与答案最相关的实体关系三元组。
要求：
1. 最多返回 5 条。
2. 每条包含 head、relation、tail 三个字段。
3. 关系尽量具体，例如：适用场景、测量范围、注意事项、包含、属于、使用步骤等。
4. 只返回 JSON，格式为：
{{
    "triples": [
        {{"head": "实体A", "relation": "关系", "tail": "实体B"}}
    ]
}}

用户问题：
{question}

参考内容：
{context}
